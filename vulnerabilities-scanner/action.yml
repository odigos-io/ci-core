name: "Vulnerability Scanner (Grype)"
description: "Scan container images or filesystem paths for vulnerabilities using Grype"

inputs:
  image:
    description: "The container image to scan, use complete image reference (e.g., 'nginx:latest' or 'myregistry.com/myimage:tag')"
    required: false
  path:
    description: "Path to the file system to scan (e.g., a directory containing source code or extracted image layers)"
    required: false
  severity-cutoff:
    description: "Comma-separated list of severity levels that will cause the workflow to fail (e.g., 'HIGH,CRITICAL' or 'CRITICAL'). Leave empty to never fail."
    required: false
    default: "HIGH,CRITICAL"
  verbosity:
    description: 'Verbosity level for the summary'
    required: false
    default: 'basic'
    type: choice
    options:
      - basic
      - detailed

outputs:
  results-json:
    description: "The raw Grype scan results in JSON format"
    value: ${{ steps.process-results.outputs.results-json }}

runs:
  using: composite
  steps:
    - name: Validate Inputs
      if: ${{ (inputs.image == '' && inputs.path == '') || (inputs.image != '' && inputs.path != '') }}
      shell: bash
      run: |
        echo "Error: You must provide either 'image' or 'path' input."
        exit 1

    - name: Install Grype
      shell: bash
      env:
        GRYPE_VERSION: "0.110.0"
      run: |
        curl -sSfL "https://github.com/anchore/grype/releases/download/v${GRYPE_VERSION}/grype_${GRYPE_VERSION}_linux_amd64.tar.gz" -o "grype_${GRYPE_VERSION}_linux_amd64.tar.gz"
        curl -sSfL "https://github.com/anchore/grype/releases/download/v${GRYPE_VERSION}/grype_${GRYPE_VERSION}_checksums.txt" -o grype_checksums.txt
        grep "grype_${GRYPE_VERSION}_linux_amd64.tar.gz" grype_checksums.txt | sha256sum -c -
        tar xzf "grype_${GRYPE_VERSION}_linux_amd64.tar.gz" grype
        sudo mv grype /usr/local/bin/grype
        rm "grype_${GRYPE_VERSION}_linux_amd64.tar.gz" grype_checksums.txt

    - name: Scan Image for Vulnerabilities
      if: ${{ inputs.image != '' && inputs.path == '' }}
      shell: bash
      run: |
        grype ${{ inputs.image }} \
          -o json \
          --only-fixed \
          --file grype-results.json

    - name: Scan Path for Vulnerabilities
      if: ${{ inputs.path != '' && inputs.image == '' }}
      shell: bash
      run: |
        grype dir:${{ inputs.path }} \
          -o json \
          --only-fixed \
          --file grype-results.json

    - name: Display in Summary
      if: always()
      id: process-results
      shell: bash
      run: |
        echo "## 🔍 Vulnerability Scan Results" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        JSON_FILE="grype-results.json"
        if [ -f "$JSON_FILE" ]; then
          SEVERITY_CUTOFF="${{ inputs.severity-cutoff }}"
          VERBOSITY="${{ inputs.verbosity }}"

          TOTAL=$(jq '[.matches // [] | .[]] | length' "$JSON_FILE")
          CRITICAL=$(jq '[.matches // [] | .[] | select(.vulnerability.severity == "Critical")] | length' "$JSON_FILE")
          HIGH=$(jq '[.matches // [] | .[] | select(.vulnerability.severity == "High")] | length' "$JSON_FILE")
          MEDIUM=$(jq '[.matches // [] | .[] | select(.vulnerability.severity == "Medium")] | length' "$JSON_FILE")
          LOW=$(jq '[.matches // [] | .[] | select(.vulnerability.severity == "Low")] | length' "$JSON_FILE")

          echo "results-json=$(cat $JSON_FILE | jq -c .)" >> $GITHUB_OUTPUT

          echo "| Severity | Count |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🔴 Critical | $CRITICAL |" >> $GITHUB_STEP_SUMMARY
          echo "| 🟠 High | $HIGH |" >> $GITHUB_STEP_SUMMARY
          echo "| 🟡 Medium | $MEDIUM |" >> $GITHUB_STEP_SUMMARY
          echo "| 🔵 Low | $LOW |" >> $GITHUB_STEP_SUMMARY
          echo "| **Total** | **$TOTAL** |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "$CRITICAL" -gt 0 ]; then
            echo "### 🔴 Critical Vulnerabilities ($CRITICAL)" >> $GITHUB_STEP_SUMMARY
            while IFS= read -r match; do
              VULN_ID=$(echo "$match" | jq -r '.vulnerability.id')
              ARTIFACT_NAME=$(echo "$match" | jq -r '.artifact.name')
              ARTIFACT_VERSION=$(echo "$match" | jq -r '.artifact.version')
              FIXED_VERSIONS=$(echo "$match" | jq -r '.vulnerability.fix.versions // [] | if length > 0 then join(", ") else "N/A" end')

              if [ "$VERBOSITY" = "basic" ]; then
                echo "- **$VULN_ID** in \`$ARTIFACT_NAME@$ARTIFACT_VERSION\` (Fixed: $FIXED_VERSIONS)"
              elif [ "$VERBOSITY" = "detailed" ]; then
                echo "- **$VULN_ID**" >> $GITHUB_STEP_SUMMARY
                echo "  - Artifact: \`$ARTIFACT_NAME@$ARTIFACT_VERSION\`" >> $GITHUB_STEP_SUMMARY
                echo "  - Fixed Versions: $FIXED_VERSIONS" >> $GITHUB_STEP_SUMMARY

                echo "  - Detection Paths:" >> $GITHUB_STEP_SUMMARY

                while IFS= read -r primary_path; do
                  [ -n "$primary_path" ] && echo "    - (primary) \`$primary_path\`" >> $GITHUB_STEP_SUMMARY
                done < <(echo "$match" | jq -r '.artifact.locations // [] | .[] | select(.annotations.evidence == "primary") | (.path // .accessPath // empty)')

                while IFS= read -r supporting_path; do
                  [ -n "$supporting_path" ] && echo "    - (supporting) \`$supporting_path\`" >> $GITHUB_STEP_SUMMARY
                done < <(echo "$match" | jq -r '.artifact.locations // [] | .[] | select(.annotations.evidence == "supporting") | (.path // .accessPath // empty)')
              fi

              echo "" >> $GITHUB_STEP_SUMMARY
            done < <(jq -c '.matches // [] | .[] | select(.vulnerability.severity == "Critical")' "$JSON_FILE")
            echo "" >> $GITHUB_STEP_SUMMARY
          fi

          if [ "$HIGH" -gt 0 ]; then
            echo "### 🟠 High Vulnerabilities ($HIGH)" >> $GITHUB_STEP_SUMMARY
            jq -r '[.matches // [] | .[] | select(.vulnerability.severity == "High")] | .[] | "- **\(.vulnerability.id)** in `\(.artifact.name)@\(.artifact.version)` (Fixed: \(.vulnerability.fix.versions // [] | if length > 0 then join(", ") else "N/A" end))"' "$JSON_FILE" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi

          if [ "$MEDIUM" -gt 0 ]; then
            echo "<details><summary>🟡 Medium Vulnerabilities ($MEDIUM)</summary>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            jq -r '[.matches // [] | .[] | select(.vulnerability.severity == "Medium")] | .[] | "- **\(.vulnerability.id)** in `\(.artifact.name)@\(.artifact.version)` (Fixed: \(.vulnerability.fix.versions // [] | if length > 0 then join(", ") else "N/A" end))"' "$JSON_FILE" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "</details>" >> $GITHUB_STEP_SUMMARY
          fi

          if [ "$LOW" -gt 0 ]; then
            echo "<details><summary>🔵 Low Vulnerabilities ($LOW)</summary>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            jq -r '[.matches // [] | .[] | select(.vulnerability.severity == "Low")] | .[] | "- **\(.vulnerability.id)** in `\(.artifact.name)@\(.artifact.version)` (Fixed: \(.vulnerability.fix.versions // [] | if length > 0 then join(", ") else "N/A" end))"' "$JSON_FILE" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "</details>" >> $GITHUB_STEP_SUMMARY
          fi

          if [ -n "$SEVERITY_CUTOFF" ]; then
            SHOULD_FAIL=false

            if [[ "$SEVERITY_CUTOFF" == *"CRITICAL"* ]] && [ "$CRITICAL" -gt 0 ]; then
              SHOULD_FAIL=true
            fi
            if [[ "$SEVERITY_CUTOFF" == *"HIGH"* ]] && [ "$HIGH" -gt 0 ]; then
              SHOULD_FAIL=true
            fi
            if [[ "$SEVERITY_CUTOFF" == *"MEDIUM"* ]] && [ "$MEDIUM" -gt 0 ]; then
              SHOULD_FAIL=true
            fi
            if [[ "$SEVERITY_CUTOFF" == *"LOW"* ]] && [ "$LOW" -gt 0 ]; then
              SHOULD_FAIL=true
            fi

            if [ "$SHOULD_FAIL" = true ]; then
              exit 1
            fi
          fi
        else
          echo "✅ No vulnerabilities found!" >> $GITHUB_STEP_SUMMARY
        fi
